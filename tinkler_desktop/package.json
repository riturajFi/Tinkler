{
  "name": "tinkler-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron frontend for the Tinkler agent backend.",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "check": "node --check electron/main.js && node --check electron/backend.js && node --check electron/ipc.js && node --check electron/preload.js && node --check renderer/backend-client.js && node --check renderer/renderer.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
